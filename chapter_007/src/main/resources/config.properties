url = jdbc:postgresql://localhost:5432/servlet
username = postgres
password = 2016

create =     CREATE TABLE IF NOT EXISTS users( \
                  id            SERIAL PRIMARY KEY, \
                  name          VARCHAR(50), \
                  login         VARCHAR(50), \
                  email         VARCHAR(50), \
                  date          TIMESTAMP, \
                  )
add =        INSERT INTO users(name, login, email, date) VALUES(?, ?, ?, ?)
update =     UPDATE users SET name = ?, login = ?, email = ? WHERE id = ?
delete =     DELETE FROM users WHERE id = ?
findById =   SELECT id, name, login, email, date FROM users WHERE id = ?
findAll =    SELECT id, name, login, email, date FROM users